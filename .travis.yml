sudo: required

language: node_js

node_js:
  - "18.16.0"

services:
  - docker

before_install:
  - docker build -t spadyala-melb/sample-app -f Dockerfile .
# script:
#   - docker run spadyala-melb/sample-app npm start
